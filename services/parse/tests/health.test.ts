import { describe, it, expect } from 'vitest';

describe('services/parse smoke', () => {
  it.skip('placeholder — full Sharp + svgo + Inkscape stack lands in Step 5', () => {
    expect(true).toBe(true);
  });
});
