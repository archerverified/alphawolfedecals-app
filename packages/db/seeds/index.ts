// Database seeds. Lands per feature PR (vehicles, default shop, etc.).
async function main(): Promise<void> {
  console.log('[db] no seeds defined yet');
}

main().catch((err: unknown) => {
  console.error(err);
  process.exit(1);
});
