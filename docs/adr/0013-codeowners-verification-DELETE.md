# THROWAWAY — Goal 0 CODEOWNERS verification (DELETE)

This file exists only to open a PR touching an ADR-0013 invariant CODEOWNERS
path (`/docs/adr/0013-*`) and confirm GitHub blocks merge until @archerverified
approves. It is closed without merging. If you are reading this on `main`,
something went wrong — delete it.
