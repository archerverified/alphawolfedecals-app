# Production-Readiness Audit — Goal 10 D2 (launch gate) — 2026-06-14

**Verdict: CONDITIONAL PASS.** Infra/ops readiness is GREEN (deploy, env, rollback, backup-restore, Sentry, DB hygiene, build, QA, repo hygiene all pass). Four **functional HIGH gaps** remain — two FIXED this gate (error boundaries), two are **launch-blocking inputs/decisions for Archer** (forgot-password flow; catalogue-template panels). Security passed separately today (`security-audit.md`). Full skill run (`production-readiness`) + live connector verification.

## Site profile

Next.js 15 App Router (React 19) on Vercel `sfo1` (Hobby), owned Postgres (Supabase us-west-1) via Prisma + two-connection RLS split, custom OTP/argon2id auth, Supabase Storage uploads, paid AI generation (fal + Anthropic, $5/day cap), referral/credits, public share/vote. Platform subdomain (DNS/apex/cert host-managed → N/A). Already serving production data. The "Next.js + owned DB + auth" profile → full functional audit.

## Connector verification (live, this session)

| Check                           | Result         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Latest **prod deploy READY**    | PASS           | `dpl_DRUxUt…` target=production, state=READY (main HEAD e5f5c00) via Vercel API                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Rollback available**          | PASS           | 2 READY production rollback candidates; instant-rollback path documented in `runbooks/rollback.md`                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Env-var completeness** (prod) | PASS           | `vercel env ls production`: UPSTASH*REDIS_REST_URL/\_TOKEN (rate limiter ✓ — resolves the D1 HUMAN-VERIFY), RESEND_FROM_EMAIL, CRON_SECRET, AI_PROVIDER, ANTHROPIC_API_KEY, FAL_KEY, DATABASE_URL_APP, PII_ENCRYPTION_KEY, AUTH_SECRET, SENTRY_DSN, POSTHOG_KEY, SUPABASE*\* — all present                                                                                                                                                                                                            |
| **Backup-restore drill**        | PASS           | `scripts/backup-restore-drill.sh`: pg_dump prod public schema (rc=0, 145 KB) → restored into a throwaway local PG16 → row counts round-tripped EXACTLY (prod users=3/shops=1/projects=17 == restored). Only notice: benign `transaction_timeout` param (Supabase PG17→local PG16). Artifact retained. **This is the standing open ops item — now closed with a verified restore.**                                                                                                                    |
| **Sentry error baseline**       | PASS (triaged) | 2 unresolved prod issues, BOTH 0-users, neither customer-path nor introduced by Goal 10: NODE-B (`/signin` next-auth client "unexpected response", 5 events, transient client edge case) + NODE-C (`/api/admin/ai-bakeoff` JSON upload rejected by the storage MIME allowlist — the security control working; admin dev-tool only). No new error class from D0–D2 changes.                                                                                                                            |
| **404-quirk root cause**        | CONFIRMED      | NOT Vercel deployment pinning. `requireAdmin()` → `notFound()` during a cold-start / session-timing window on the operator-only `/api/admin/ai-bakeoff` (always-cold lambda). Benign, self-healing, zero customer impact, fail-closed ledger (a 404'd call spends nothing). Mitigation shipped: custom `not-found.tsx` gives the 404 a body (removes the "route not deployed" ambiguity); runbook adds a `/health`-SHA readiness gate before post-deploy admin calls + a Sentry alert recommendation. |
| **UptimeRobot monitor**         | HUMAN-VERIFY   | `/health` endpoint exists (edge, `{status,commit}`). Confirm an external UptimeRobot monitor hits `https://alphawolfedecals-app-web.vercel.app/health` on a schedule with alert routing (dashboard-side, not codebase-verifiable here).                                                                                                                                                                                                                                                               |

## Functional audit (codebase, independent fresh-context run)

**Shipped & solid (PASS):** clean build (`pnpm --filter @alphawolf/web build` exit 0, zero warnings, 40 routes); 0 debug artifacts / TODO/FIXME; stranger-test README; lockfile + Node/pnpm pins committed; thorough DB schema (every queried column indexed; comprehensive NOT NULL/UNIQUE/FK/CHECK; partial-unique idempotency on credits/generation); strong QA (15 Playwright e2e + 11 RLS integration specs covering signup→vehicle→editor→submit→fulfil and brief→generate→final); empty states + sonner success toasts present; `/health` endpoint; tint/AI disclaimer present (wording flagged for Archer).

**Real open gaps:**
| # | Gap | Sev | Disposition |
|---|---|---|---|
| 1 | **No `error.tsx`/`global-error.tsx`/`not-found.tsx` anywhere** → any failed server fetch = white-screen-of-death | High | **FIXED this gate** — added root `error.tsx` (Sentry-reporting, retry+home), `global-error.tsx` (root-layout fallback), `not-found.tsx` (styled 404). Catches errors app-wide (Next boundaries bubble). |
| 2 | Slow-3G blank screens on server-rendered list pages (no streaming skeletons) | Med | Follow-up — error boundaries land first; add `loading.tsx`/Suspense skeletons post-launch (only `/editor` has one today). |
| 3 | Silent error-swallow in vehicle search + generation poll (catch → empty, no message) | Med | Follow-up — add `toast.error` + inline retry (cheap; logged). |
| 4 | **No forgot-password flow** (backend OTP capability exists; no UI route) | High | **LAUNCH-BLOCKING INPUT (Archer)** — product decision + a reset route/email. Locked-out users have no self-serve recovery. Backend half-wired (`password_reset_*` purposes). |
| 5 | Legal `/terms` + `/privacy` exist but **unreachable** (no footer/links) + non-binding stub copy | High | Reachability FIXED in D4 (footer + links); binding COPY is the D4 Archer placeholder-flag (never fabricated). |
| 6 | **Editor non-functional on the 3 curated catalogue templates** (no `vehicle_panels`; only the Ford Transit works) | High | **KNOWN PRE-EXISTING LAUNCH BLOCKER** (Goal-4 blocker #1, ADR-0014 inv 12). Authoring panels is Goal-6/8 paneling work — Goal 8 is DEFERRED. Must be resolved (panels authored) before catalogue vehicles are offered, or the catalogue limited to paneled vehicles at launch. Archer decision. |
| 7 | Customer orders created with `ownerShopId=null` → never reach a shop dashboard | Med (latent) | By design for the MVP (locator "download spec pack, take to a shop" handoff, not in-app routing). Confirm intended; becomes a bug only if in-app routing is advertised. |
| 8 | No cookie-consent banner | Low | HUMAN-VERIFY — only functional cookies today (auth/CSRF/voter_token); add a banner only if EU traffic + non-essential cookies. |

## Launch-readiness disposition

- **Ops/infra axis: GREEN** — deploy, rollback, backup-restore (verified), env, Sentry, DB/repo/build hygiene, QA all pass.
- **Functional axis: BLOCKED on two items requiring Archer** — (#4) forgot-password recovery and (#6) catalogue-template panels (the long-standing Goal-4 blocker). Error boundaries (#1) FIXED; legal reachability (#5) fixed in D4; the rest are documented follow-ups.
- These two blockers are carried explicitly into the D7 launch checklist and the final GO/NO-GO.
