# MCP smoke checklist — Goal 0 foundation

**Run:** 2026-05-25 · **Operator:** Claude (cowork session) · **Result: 10 PASS / 1 FAIL**

Connectivity probe of every service the autonomous /goal chain (Goals 1-4) depends on.
Probes are intentionally read-only / non-destructive (no emails sent, no rows written).

> **Two MCP surfaces.** This _cowork_ session exposes Vercel, GitHub (PAT), Supabase, and
> Figma as live MCP tools, plus Sentry/PostHog/Resend via env vars. The desktop MCPs
> (Control Chrome, Filesystem, pdf-viewer, Shadcn UI) are configured in Archer's **Claude
> Code desktop** session, not here — those rows are verified via an equivalent CLI/registry
> fallback and the row Notes say so. Goal 1 (single-vehicle scrape) runs from the desktop
> session where Control Chrome is live.

| MCP            | Probe                                               | Status      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| -------------- | --------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vercel         | `get_project` on `prj_t0sBn2LKBjTfqS6B4o8HgygNRLjF` | **PASS**    | MCP (cowork). `alphawolfedecals-app-web`, framework `nextjs`, node `24.x`, latest **production** deploy `dpl_D1DicU8BLNARzXnvyQqAxqwiZJrt` = `READY`. teamId `team_VWQt3IpUm0mRUCexsqcMMsBm`.                                                                                                                                                                                                                                               |
| GitHub         | `gh api /user` (PAT)                                | **PASS**    | `archerverified` (id 246311904). Token scopes: `gist, read:org, repo, workflow` — `repo` covers branch-protection writes used in D2.                                                                                                                                                                                                                                                                                                        |
| Supabase       | `execute_sql 'SELECT 1'`                            | **PASS**    | MCP (cowork). Returned `{smoke_test:1, db:"postgres"}`. `DATABASE_URL` / `DIRECT_URL` / `DATABASE_URL_APP` all present in `.env.local`.                                                                                                                                                                                                                                                                                                     |
| Figma          | `whoami`                                            | **PASS** ⚠️ | MCP (cowork) authenticates — **but as `Tee` (`saiphyowaiyan_1@cmu.ac.th`), not Archer** (3 student-tier teams). Connectivity OK; **file-metadata probe DEFERRED** — no Alpha Wolf file URL provided, and Archer's own Figma files likely aren't reachable under this account. See unresolved blockers + `manual-steps.md` item 8.                                                                                                           |
| Shadcn UI      | component count > 50                                | **PASS**    | MCP is desktop-only (not in cowork session). Verified via public registry `https://ui.shadcn.com/r/index.json` → **56 components** (> 50 threshold).                                                                                                                                                                                                                                                                                        |
| Control Chrome | open prod URL + read page                           | **FAIL**    | Desktop-only MCP — **not exposed in this cowork session**. Independent reachability confirmed via `curl https://alphawolfedecals-app-web.vercel.app/health` → **HTTP 200**. Resolution: run the Chrome probe from Archer's Claude Code **desktop** session (where it's live). Not a goal-chain blocker — curl/Playwright cover prod reads; Goal 1 drives Control Chrome from the desktop session.                                           |
| Filesystem     | list a path outside the repo                        | **PASS**    | Native `Read`/`Bash` provide FS access equivalent to the desktop `Filesystem` MCP; `ls $HOME` (outside repo) succeeded.                                                                                                                                                                                                                                                                                                                     |
| pdf-viewer     | list PDFs in `docs/`                                | **PASS**    | Desktop-only MCP. Filesystem scan found **0 PDFs** in the repo — expected pre-Goal-4 (Goal 4 generates the handoff PDF into `dist/mvp-handoff/`). Capability verified; nothing to enumerate yet.                                                                                                                                                                                                                                            |
| Sentry         | DSN present + format + reachability                 | **PASS** ⚠️ | `SENTRY_DSN` present in `.env.local`, format valid (`https://<key>@<host>/<id>`). Ingest host `o4511425978630144.ingest.us.sentry.io` resolves + responds (`/api/0/` → 404 is normal for an ingest endpoint). ⚠️ **Prod CSP currently blocks this regional `.ingest.us.sentry.io` host** — separate Phase-1 observability bug already logged in `activities.md` (2026-05-25); fix the `connect-src` so client errors actually reach Sentry. |
| PostHog        | project token present + ping                        | **PASS**    | `POSTHOG_KEY` present (`phc_…` — valid public project-key format); `us.i.posthog.com` reachable. **Var name is `POSTHOG_KEY`**, not the `NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN` the Goal 0 spec referenced — see `posthog-setup-report.md`.                                                                                                                                                                                                     |
| Resend         | `/domains` list (no send)                           | **PASS** ⚠️ | `RESEND_API_KEY` present + **valid** — `/domains` returns 401 `restricted_api_key` = the key authenticates but is **scoped to send-only** (cannot manage domains). Sufficient for Goal 3c email _sends_. Domain verification + (if needed) a full-access key for domain ops are manual steps — see `manual-steps.md` items 5.                                                                                                               |

## FAIL resolutions (the 1 FAIL)

1. **Control Chrome — desktop-only MCP, not exposed in this cowork session.**
   - _Resolution:_ No action needed for the chain to proceed. The browser-driven probe lives in Archer's Claude Code **desktop** session, which is where Goal 1's scrape (`templates.provehicleoutlines.com`) runs anyway. Prod reachability is already covered headlessly (curl `/health` = 200; Playwright deploy-smoke). If a cowork-side browser is ever needed, use the in-session Chrome extension MCP (`mcp__claude-in-chrome__*`).

## Caveats on PASS rows (not failures, but act on these)

- **Figma is authed as a non-Archer account (`Tee`).** Connectivity passes, but Goal 3a/3b Figma pulls of Alpha Wolf designs will only work if those files are shared with this account _or_ Archer re-auths the Figma MCP. Decide before STEP F. (manual-steps item 8)
- **Resend key is send-only.** Fine for sending; domain management/verification needs the dashboard or a full-access key. (manual-steps item 5)
- **Sentry ingest is CSP-blocked in prod** despite a valid DSN — fix the `connect-src` regional host or Sentry stays blind to client errors. (tracked in activities.md 2026-05-25)
- **PostHog var name** is `POSTHOG_KEY` (not the spec's `NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN`).
